doctype html
html
    head
        include head.pug
        link(rel="stylesheet" href="/#{root}/css/content.css?ver=#{version}")
    body
        include common.pug
        include appbar.pug
        include table-contents.pug
        include row-adder.pug
        include table-info.pug
        include column-selector.pug

        input#version(type=hidden value="#{version}")

        script(type='module' src='/#{root}/dist/js/content.js?ver=#{version}')
